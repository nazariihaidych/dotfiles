# Secondary terminal config — Ghostty is primary (see ../ghostty/).
# Trimmed to actual overrides; see `kitty +kitten themes` / the shipped
# kitty.conf template (installed alongside kitty itself) for every other option.

font_size 18.0
initial_window_width  960
initial_window_height 600
hide_window_decorations no
background_opacity 0.65
